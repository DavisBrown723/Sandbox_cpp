#pragma once

namespace sandbox {
    namespace types {

        enum class TargetingSystem {
            Visual = 1,
            IR = 2,
            Laser = 4,
            Radar = 8
        };

    }
}